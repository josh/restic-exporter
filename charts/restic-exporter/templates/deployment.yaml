{{- include "restic-exporter.validate" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "restic-exporter.fullname" . }}
  labels:
    {{- include "restic-exporter.labels" . | nindent 4 }}
spec:
  replicas: 1
  progressDeadlineSeconds: {{ .Values.progressDeadlineSeconds }}
  {{- with .Values.strategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "restic-exporter.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- if or .Values.prometheusScrape .Values.podAnnotations }}
      annotations:
        {{- if .Values.prometheusScrape }}
        prometheus.io/scrape: "true"
        prometheus.io/port: {{ include "restic-exporter.containerPort" . | quote }}
        {{- end }}
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
      labels:
        {{- merge (include "restic-exporter.selectorLabels" . | fromYaml) (deepCopy (default dict .Values.podLabels)) | toYaml | nindent 8 }}
    spec:
      serviceAccountName: {{ include "restic-exporter.serviceAccountName" . }}
      {{- with .Values.priorityClassName }}
      priorityClassName: {{ . | quote }}
      {{- end }}
      automountServiceAccountToken: false
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: restic-exporter
          image: {{ include "restic-exporter.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            # An empty address binds dual-stack with an IPv4 fallback; the default "[::]" needs IPv6.
            - -listen-address=
            {{- if .Values.exporter.verbose }}
            - -verbose
            {{- end }}
          env:
            {{- with .Values.restic.repository }}
            - name: RESTIC_REPOSITORY
              value: {{ . | quote }}
            {{- end }}
            - name: RESTIC_CACHE_DIR
              value: {{ .Values.cache.path | quote }}
            - name: RESTIC_EXPORTER_REFRESH_INTERVAL
              value: {{ .Values.exporter.refreshInterval | quote }}
            - name: RESTIC_EXPORTER_INCLUDE_PATHS
              value: {{ .Values.exporter.includePaths | quote }}
            {{- if .Values.restic.ageIdentity.enabled }}
            - name: RESTIC_AGE_IDENTITY_FILE
              value: {{ .Values.restic.ageIdentity.mountPath | quote }}
            - name: RESTIC_PASSWORD_COMMAND
              value: restic-age-key password
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- if or .Values.restic.existingSecret .Values.extraEnvFrom }}
          envFrom:
            {{- with .Values.restic.existingSecret }}
            - secretRef:
                name: {{ . | quote }}
            {{- end }}
            {{- with .Values.extraEnvFrom }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ include "restic-exporter.containerPort" . }}
              protocol: TCP
          startupProbe:
            httpGet:
              path: /readyz
              port: http
            {{- toYaml .Values.startupProbe | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: cache
              mountPath: {{ .Values.cache.path | quote }}
            {{- if .Values.restic.ageIdentity.enabled }}
            - name: age-identity
              mountPath: {{ .Values.restic.ageIdentity.mountPath | quote }}
              subPath: identity
              readOnly: true
            {{- end }}
      volumes:
        - name: cache
          {{- with .Values.cache.volumeSource }}
          {{- toYaml . | nindent 10 }}
          {{- else with .Values.cache.sizeLimit }}
          emptyDir:
            sizeLimit: {{ . }}
          {{- else }}
          emptyDir: {}
          {{- end }}
        {{- if .Values.restic.ageIdentity.enabled }}
        - name: age-identity
          secret:
            secretName: {{ include "restic-exporter.ageIdentitySecretName" . | quote }}
            items:
              - key: {{ .Values.restic.ageIdentity.secretKey }}
                path: identity
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
