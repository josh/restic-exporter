apiVersion: v1
kind: Service
metadata:
  name: {{ include "restic-exporter.fullname" . }}
  labels:
    {{- include "restic-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "restic-exporter.selectorLabels" . | nindent 4 }}
